//Get Random element from array
let ages = [12, 32, 93, 33, 28, 58];

Math.round(Math.random() * ages.length);

//get a random from index/element of the array
ages[Math.round(Math.random() * ages.length)];
